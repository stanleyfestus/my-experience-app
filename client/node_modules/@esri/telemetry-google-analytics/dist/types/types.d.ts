import { ITelemetryPluginOptions } from '@esri/telemetry';
export interface IUaTracker {
    event: (data: any) => {
        send: any;
    };
}
export interface IGA4Options extends ITelemetryPluginOptions {
    measurementIds?: string[];
    uaTracker?: IUaTracker;
    dimensions?: Record<string, any>;
    metrics?: Record<string, any>;
}
