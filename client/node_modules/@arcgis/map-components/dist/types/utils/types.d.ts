import type { VNode } from "../stencil-public-runtime";
import type { Nil } from "@arcgis/components-utils";
export type VNodeChild = Nil | VNode | VNodeChild[] | string | false;
export type LitEventEmitter<T = undefined> = undefined extends T ? {
    emit(): CustomEvent<undefined>;
} : {
    emit(payload: T): CustomEvent<T>;
};
