import { Series } from "@amcharts/amcharts5";
/**
 * Gets x-axis field name. Honored is rotated
 * @param series The series instance
 */
export declare function getXFieldFromSeriesDataFields(series: Series): string;
/**
 * Gets y-axis field name. Honored is rotated.
 * @param series The series instance
 */
export declare function getYFieldFromSeriesDataFields(series: Series): string;
