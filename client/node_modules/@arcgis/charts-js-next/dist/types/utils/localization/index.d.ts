import type { Root } from "@amcharts/amcharts5";
import { DateFormatter, NumberFormatter } from "@amcharts/amcharts5";
import type { ILocale } from "@amcharts/amcharts5/.internal/core/util/Language";
import { WebChartTypes } from "@arcgis/charts-spec";
export declare function fetchChartLocaleStrings(locale?: string): Promise<ILocale>;
/**
 * Generates a number or date formatter based on the chart's locale and the intl options
 * @param props.root The root
 * @param props.type The type of formatter to generate
 * @param props.intlOptions The intl options for the formatter
 * @param props.formatLocale The format locale
 */
export declare function getFormatter<FormatterType extends DateFormatter | NumberFormatter>(props: {
    root: Root;
    type: typeof WebChartTypes.DateAxisFormat | typeof WebChartTypes.NumberAxisFormat;
    intlOptions?: Intl.NumberFormatOptions;
    formatLocale?: string;
}): FormatterType;
