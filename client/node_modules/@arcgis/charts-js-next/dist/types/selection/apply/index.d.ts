import type { SerialChart } from "@amcharts/amcharts5";
import { LineSeries } from "@amcharts/amcharts5/xy";
import type { ChartOptions } from "@arcgis/charts-js-common";
import type { QueryObject, ErrorStrings } from "@arcgis/charts-shared-utils";
import { SelectionSource } from "@arcgis/charts-shared-utils";
import type { WebChart, WebGaugeChart, WebChartDataTypes } from "@arcgis/charts-spec";
import type { ApplySelectionOnSeriesProps } from "./interfaces";
/**
 * Apply a selection on a specific series
 * @param props
 */
export declare function applySelectionOnSeries(props: ApplySelectionOnSeriesProps): void;
/**
 * Apply a selection on a chart
 * @param props
 */
export declare function applySelectionOnChart(props: {
    chart: SerialChart;
    chartConfig: WebChart | WebGaugeChart;
    data?: WebChartDataTypes;
    queryObject: QueryObject;
    options?: ChartOptions;
    dataWasUpdated?: boolean;
    rotationChanged?: boolean;
    sortChanged?: boolean;
    groupOfSlicesWasUpdated?: boolean;
    errorStrings: ErrorStrings;
}): Promise<void>;
/**
 * Finalizes the selection:
 * - gathers the selection items (list of data contexts)
 * - gathers the selection indexes (list of series indexes and sub-indexes)
 * - gathers the selection OIDs
 * - sends the selection event
 * @param series The series on which the selection is applied
 * @param selectionSource The source of the selection
 */
export declare function finalizeSelection(series: LineSeries, selectionSource: SelectionSource): Promise<void>;
