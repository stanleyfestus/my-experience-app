import type { XYChart } from "@amcharts/amcharts5/xy";
/**
 * Adds listeners to the chart to handle the selection:
 * - by range
 * - by hit, but only to clear the selection. The selection by hit on a an element (bullet...)
 * is handled by the functions initializing those elements (e.g. initializeScatterPlotMarkers())
 * @param chart The chart
 */
export declare function addSelectionOnChart(chart: XYChart): Promise<void>;
