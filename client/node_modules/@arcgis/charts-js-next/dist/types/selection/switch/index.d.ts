import type { Chart } from "@amcharts/amcharts5";
/**
 * Switches selection already done on the chart instance.
 * @param chart The chart
 */
export declare function switchSelection(chart: Chart): Promise<void>;
