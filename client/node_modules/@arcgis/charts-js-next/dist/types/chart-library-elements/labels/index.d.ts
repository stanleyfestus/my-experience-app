import type { Container, Root } from "@amcharts/amcharts5";
import { Label, Template } from "@amcharts/amcharts5";
import type { LabelProperties } from "@arcgis/charts-js-common";
import type { WebChartText } from "@arcgis/charts-spec";
export declare function applyLabelDefaults(label: Label, defaults?: LabelProperties): void;
/**
 * Creates a label with default properties and adds it to the parent container when provided.
 * @param props.root The root
 * @param props.parent The parent container
 * @param props.defaults The default settings for the label
 * @param props.index The index to insert the label at. If not provided, the label is appended to the end of the parent's children
 * @param props.id An id to set on the label
 */
export declare function initializeLabel(props: {
    root: Root;
    parent?: Container;
    defaults?: LabelProperties;
    index?: number;
    id?: string;
}): Label;
/**
 * Update the label
 * @param label the label to update
 * @param labelConfig the config of the label
 * @param defaults default settings for label
 * @param skipSettingText in some cases, label text is set already so we can skip setting it
 */
export declare function updateLabel(props: {
    label: Label | Template<Label>;
    labelConfig?: WebChartText;
    defaults?: LabelProperties;
    skipSettingText?: boolean;
}): void;
