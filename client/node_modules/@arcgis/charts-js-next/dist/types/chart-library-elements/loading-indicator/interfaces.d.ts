import { Container, Button, Graphics } from "@amcharts/amcharts5";
import { Animation } from "@amcharts/amcharts5/.internal/core/util/Entity";
export interface LoaderElements {
    loaderSpinner: Graphics;
    loaderContainer: Container;
    closeLoaderButton: Button;
    spinnerAnimation: Animation<number | undefined>;
}
export interface LoaderHandle {
    showLoader: (show: boolean) => void;
    hideLoader: () => void;
    showCloseLoaderButton: (show: boolean) => void;
}
