import type { Chart, Root } from "@amcharts/amcharts5";
import type { LoaderHandle } from "./interfaces";
/**
 * Initialize the loader and defines the nested functions
 * @param root The chart's root
 * @param chart The chart
 */
export declare function initializeLoader(root: Root, chart: Chart): LoaderHandle;
/**
 * Hides the loader when the chart is done rendering
 * @param series
 * @param hideLoaderHandle
 */
export declare function chartReadyCheck(root: Root, hideLoaderHandle: LoaderHandle["hideLoader"]): void;
