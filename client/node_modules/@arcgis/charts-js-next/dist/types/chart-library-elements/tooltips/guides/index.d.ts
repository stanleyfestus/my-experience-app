import type { CommonStrings } from "@arcgis/charts-shared-utils";
import type { GuideTooltipFormatCallback } from "@arcgis/charts-js-common";
import type { Axis, AxisRenderer, IAxisDataItem } from "@amcharts/amcharts5/xy";
import { DataItem } from "@amcharts/amcharts5";
/**
 * Initializes the guide tooltip for an axis.
 * @param props.axis The axis containing the guides that need their tooltip to be initialized
 * @param props.guide The guide to initialize the tooltip for
 * @param props.guideIndex The index of the guide
 */
export declare function initializeGuideTooltip(props: {
    axis: Axis<AxisRenderer>;
    guide: DataItem<IAxisDataItem>;
    guideIndex: number;
}): void;
/**
 * Updates the axis guides tooltip formatter, by using the custom formatter provided through the chart options
 * or generating a default one.
 * @param props.axis The axis containing the guides that need their label to be formatted
 * @param props.guideTooltipFormatter A custom formatter for the guides' label
 * @param props.commonStrings List of t9n strings
 */
export declare function updateGuideTooltipFormatter(props: {
    axis: Axis<AxisRenderer>;
    guideTooltipFormatter?: GuideTooltipFormatCallback;
    commonStrings: CommonStrings;
}): void;
