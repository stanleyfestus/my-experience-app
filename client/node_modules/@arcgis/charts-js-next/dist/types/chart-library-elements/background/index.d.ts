import { Chart, Root } from "@amcharts/amcharts5";
import { EsriColor } from "../../types";
/**
 * Initialize the chart background color.
 * @param root The chart's root
 * @param chart The chart
 */
export declare function initializeChartBackground(root: Root, chart: Chart): void;
/**
 * Updates the chart background color.
 * @param chart The chart
 * @param backgroundColor The background color to apply
 */
export declare function updateChartBackground(chart: Chart, backgroundColor?: EsriColor): void;
