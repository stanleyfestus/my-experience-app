import type { Root, SerialChart } from "@amcharts/amcharts5";
import { Container } from "@amcharts/amcharts5";
import type { ContainerProperties } from "@arcgis/charts-js-common";
/**
 * Initializes a container
 * @param props.root The root
 * @param props.defaults The default settings for the container
 * @param props.parent The parent of the container
 * @param props.id The id of the container
 */
export declare function initializeContainer(props: {
    root: Root;
    defaults?: ContainerProperties;
    parent?: Container;
    id?: string;
    index?: number;
}): Container;
export declare function updateContainer(container: Container, disabled: boolean): void;
/**
 * Set default paddings for chart containers
 * @param chart
 */
export declare function initializeChartContainersPaddings(chart: SerialChart): void;
