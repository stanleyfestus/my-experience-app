import type { LineSeries } from "@amcharts/amcharts5/xy";
import type { CurveFactory } from "d3-shape";
import type { RESTRendererType, WebChartLineChartSeries, WebChartRadarChartSeries } from "@arcgis/charts-spec";
import type { ChartSubType, SeriesSymbolsMap } from "@arcgis/charts-shared-utils";
/**
 * retrieves the curve factor for a series
 * @param props.isRadarLineSeries
 * @param props.lineSmoothed
 * @param props.isRotated
 * @returns
 */
export declare function getCurveFactory(props: {
    isRadarLineSeries: boolean;
    isLineSmoothed?: boolean;
    isRotated?: boolean;
}): CurveFactory;
/**
 * Styles a line series and its markers
 * This is used by the line chart and radar chart
 * @param props.series the line series
 * @param props.seriesConfig the series configuration
 * @param prop.seriesIndex the index of the series
 * @param prop.numberOfSeries the number of series in the chart
 * @param prop.colorMatch whether the color should match the renderers
 * @param prop.chartSubType the chart subtype
 * @param prop.rendererType the renderer type
 * @param prop.seriesIndexMapper the map object containing the series index for the series id
 * @param prop.seriesSymbolMapForEvent the map containing the series symbols for the event
 */
export declare function updateLineSeriesStyle(props: {
    series: LineSeries;
    seriesConfig: WebChartLineChartSeries | WebChartRadarChartSeries;
    seriesIndex: number;
    numberOfSeries: number;
    colorMatch: boolean;
    chartSubType: ChartSubType;
    rendererType?: RESTRendererType;
    seriesIndexMapper: Map<string, number>;
    seriesSymbolMapForEvent: SeriesSymbolsMap;
    isRotated: boolean;
}): void;
