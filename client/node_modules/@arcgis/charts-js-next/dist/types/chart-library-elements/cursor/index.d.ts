import type { Chart, Root } from "@amcharts/amcharts5";
import type { WebChartCursorCrosshair } from "@arcgis/charts-spec";
import type { CursorRange } from "@arcgis/charts-shared-utils";
import { ActionModes } from "@arcgis/charts-shared-utils";
/**
 * Creates cursor on the chart if its compatible
 * Only for XYChart
 * @param chart
 */
export declare function initializeChartCursor(root: Root, chart: Chart): void;
/**
 * Updates the style and visibility of the cursor's crosshair.
 * @param props.root The root
 * @param props.chart The chart
 * @param props.cursorCrosshair The cursor crosshair style
 */
export declare function updateChartCursor(props: {
    root: Root;
    chart: Chart;
    cursorCrosshair?: WebChartCursorCrosshair;
}): void;
/**
 * Updates the chart cursor behavior, by either using the requested mode or by applying the current one updated with the rotation
 * @param props.chart The chart
 * @param props.actionMode The action mode (selection, zoom...)
 * @param props.cursorRange The cursor range (multi-axes, x-axis...)
 */
export declare function updateChartCursorBehavior(props: {
    chart: Chart;
    actionMode?: ActionModes;
    cursorRange?: CursorRange;
}): void;
