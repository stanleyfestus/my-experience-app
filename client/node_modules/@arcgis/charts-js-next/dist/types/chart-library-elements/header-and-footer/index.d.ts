import type { Chart, Root } from "@amcharts/amcharts5";
import type { XYChart } from "@amcharts/amcharts5/xy";
import type { WebChartText } from "@arcgis/charts-spec";
import type { HeaderProps } from "./interfaces";
/**
 * Initializes the header container and its children
 * @param root The root
 * @param chart The chart
 */
export declare function initializeHeaderContainer(root: Root, chart: XYChart): void;
/**
 * Updates the header container and its labels
 * @param headerElements The title and subTitle elements
 * @param titlesConfig The title and subTitle config
 */
export declare function updateHeaderContainer(chart: Chart, titlesConfig?: HeaderProps): void;
/**
 * Initializes the footer container and its label
 * @param chart The chart
 */
export declare function initializeFooterContainer(chart: Chart): void;
/**
 * Updates the footer container and its label
 * @param chart The chart
 * @param footerConfig The footer config
 */
export declare function updateFooterContainer(chart: XYChart, footerConfig?: WebChartText): void;
