import type { WebChartDataItem } from "./data-source";
import type { WebChartAxis } from "./web-chart";
export declare const OverlayTerms: {
    readonly Mean: "Mean";
    readonly Median: "Median";
    readonly StdDev: "Standard Deviation";
    readonly Normal: "Normal Distribution";
    readonly Trendline: "Trendline";
};
export type OverlayTerms = (typeof OverlayTerms)[keyof typeof OverlayTerms];
/**+
 * Represents a subset of the event payload carrying the chart's axes bounds.
 * See event `arcgisChartsAxesMinMaxChange`.
 */
export interface CalculatedMinMaxBoundsPayload {
    isXAxis: boolean;
    isSecondValueAxis?: boolean;
    minimum: WebChartAxis["minimum"];
    maximum: WebChartAxis["maximum"];
}
/**
 * Represents the payload of the event sent when a chart is removed from the chart list (component arcgis-charts-config-view-charts).
 */
export interface ChartDeletePayload {
    chartId: string;
}
/**
 * Represents the payload of the event sent when an item is toggled in the chart's legend (slice or series).
 */
export interface LegendItemVisibility {
    visible: boolean;
    seriesId?: string;
    overlayType?: OverlayTerms;
    sliceId?: string;
    sliceData?: WebChartDataItem;
}
