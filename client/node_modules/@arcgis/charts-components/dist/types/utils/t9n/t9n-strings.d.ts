import type { CommonStrings, ErrorStrings } from "@arcgis/charts-shared-utils";
/**
 * Fetches the error strings for a component
 * @param htmlElement The component's HTML element
 */
export declare function fetchErrorsStrings(htmlElement: HTMLElement): Promise<ErrorStrings>;
/**
 * Fetches the error strings for a component
 * @param htmlElement The component's HTML element
 */
export declare function fetchCommonStrings(htmlElement: HTMLElement): Promise<CommonStrings>;
