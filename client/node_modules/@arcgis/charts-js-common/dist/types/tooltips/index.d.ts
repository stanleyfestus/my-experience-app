import type { CommonStrings } from "@arcgis/charts-shared-utils";
import type { IField, WebChart } from "@arcgis/charts-spec";
/**
 * Returns the primary text if not empty, the default text otherwise
 * @param defaultText
 * @param primaryText
 */
export declare function getTooltipHeadingText(defaultText: string, primaryText?: string): string;
/**
 * Returns the tooltip to display for a bar/line by field.
 * or the axis title itself.
 * @param props.config The chart's config
 * @param props.fieldName The field name from the hovered bar/marker
 * @param props.commonStrings The common strings containing the localized text
 */
export declare function getTooltipLabelForBarLineFromFields(props: {
    config: WebChart;
    fieldName?: string;
    commonStrings: CommonStrings;
}): string;
/**
 * The tooltip y label is the label associated to the y-axis. It can be the name of the field used by the axis, its alias,
 * or the axis title itself.
 * @param props
 */
export declare function getTooltipYLabel(props: {
    config: WebChart;
    fieldList?: IField[];
    objectIdField: string;
    seriesIndex: number;
    isStacked100: boolean;
    commonStrings: CommonStrings;
}): string;
