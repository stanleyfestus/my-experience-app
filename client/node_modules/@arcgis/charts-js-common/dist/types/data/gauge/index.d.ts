import type { WebChartQuery, WebGaugeChart } from "@arcgis/charts-spec";
import type { QueryObject, AbortControllerKeyType, DataInfo } from "@arcgis/charts-shared-utils";
import type { ChartOptions } from "../../interfaces";
/**
 * Fetches the gauge data and returns it.
 * @param props
 */
export declare function fetchFinishedGaugeData(props: {
    queryObject?: QueryObject;
    chartConfig: WebGaugeChart;
    queryConfig: WebChartQuery;
    options?: ChartOptions;
    abortKey?: AbortControllerKeyType;
}): Promise<DataInfo>;
/**
 * Performs a query to fetch the gauge min/max bounds, when `minimumFromField` or `maximumFromField` is provided
 * @param props.chartConfig The gauge chart configuration
 * @param props.queryObject The query object (layer or feature layer view)
 * @param props.queryConfig The query configuration
 */
export declare function fetchStatisticGaugeBounds(props: {
    chartConfig: WebGaugeChart;
    queryObject: QueryObject;
    queryConfig: WebChartQuery;
}): Promise<{
    minimumFromData: number | undefined;
    maximumFromData: number | undefined;
}>;
