import { PieChart } from "@amcharts/amcharts4/charts";
/**
 * Add an event listener after a Hit or DoubleHit on the chart.
 * It handles the multi-selection to select multiple slices.
 * @param chart
 */
export declare function addHitSelectionOnPieChart(chart: PieChart): void;
