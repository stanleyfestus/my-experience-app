import { XYChart, PieChart } from "@amcharts/amcharts4/charts";
import type { Sprite } from "@amcharts/amcharts4/core";
/**
 * Add a listener on the chart for range and hit selection.
 * Add an event listener on the chart's event "up".
 * @param chart The chart
 */
export declare function addRangeSelectionHandlerToChart(chart: XYChart): void;
/**
 * Add an event listener on Hit and Double Hit on a chart.
 * @param chart The chart
 */
export declare function addHitSelectionHandlerToChart(chart: XYChart): void;
/**
 * Add an event listener on Hit and Double Hit on a chart's series
 * @param chart The chart
 * @param element The series' element on which to add the handler (e.g. the column template from a column series)
 */
export declare function addHitSelectionHandlerToSeries(chart: XYChart, element: Sprite): void;
/**
 * Inverses all the selected elements on a chart
 * @param chart
 */
export declare function switchChartSelection(chart: PieChart | XYChart): Promise<void>;
/**
 * Disables all selection features on a chart and the cursor
 * @param chart
 */
export declare function removeSelectionEvents(chart: PieChart | XYChart): void;
/**
 * Enables all selection features on a chart and the cursor
 * @param chart
 */
export declare function addSelectionEvents(chart: PieChart | XYChart): void;
