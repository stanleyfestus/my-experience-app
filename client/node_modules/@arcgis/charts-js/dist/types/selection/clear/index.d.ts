import { SerialChart } from "@amcharts/amcharts4/charts";
import { SelectionSource } from "@arcgis/charts-shared-utils";
/**
 * Clears selection from the chart.
 * @param chart The chart to clear
 * @param selectionSource What triggered the clearing
 * @param clearOnlyVisibleSeries Whether to clear only the visible series
 * @param disposeStyleAndSelection Whether to stop the style and selection callbacks to be executed after clearing
 */
export declare function clearSelection(props: {
    chart: SerialChart;
    selectionSource?: SelectionSource;
    clearOnlyVisibleSeries?: boolean;
    disposeStyleAndSelection?: boolean;
}): void;
