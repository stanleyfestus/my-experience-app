import type { Label } from "@amcharts/amcharts4/core";
import type { ColumnSeries } from "@amcharts/amcharts4/charts";
import { WebChartStackedKinds } from "@arcgis/charts-spec";
import type { DataLabelUpdateProps } from "../interfaces";
/**
 * Looks for an existing series data label. Bar chart has two data labels, one being displayed while in side-by-side and
 * and another in stack mode. One only will be displayed and the other hidden.
 * @param series The chart's series
 * @param stackedType The chart's stack type
 */
export declare function findDataLabels(series: ColumnSeries, stackedType?: WebChartStackedKinds): {
    labelToRender: Label | undefined;
    labelToHide: Label | undefined;
};
/**
 * Updates a bar series data labels style and format.
 * @param props.series The chart's series
 * @param props.stackedType The chart's stack type
 * @param props.isRotated A boolean indicating if the chart is rotated
 * @param props.hideOversizedStackedLabels A boolean indicating if the stacked labels should be hidden when oversized
 */
export declare function updateBarSeriesDataLabel(props: DataLabelUpdateProps): void;
