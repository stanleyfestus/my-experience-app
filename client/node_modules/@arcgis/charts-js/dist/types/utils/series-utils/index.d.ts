import { Series } from "@amcharts/amcharts4/charts";
import { WebChartTypes } from "@arcgis/charts-spec";
/**
 * Gets x-axis field name. Honored is rotated
 * @param series
 */
export declare function getXFieldFromSeriesDataFields(series: Series): string;
/**
 * Gets y-axis field name. Honored is rotated.
 * @param series
 */
export declare function getYFieldFromSeriesDataFields(series: Series): string;
/**
 * Gets series type series from chart memory storage config.
 * @param series
 */
export declare function getSeriesTypeFromAMChartsSeries(series: Series, index?: number): WebChartTypes | undefined;
