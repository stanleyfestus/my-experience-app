import { ILocale } from "@amcharts/amcharts4/.internal/core/utils/Language";
import { Chart, XYChart } from "@amcharts/amcharts4/charts";
import { Percent } from "@amcharts/amcharts4/core";
import { WebMapWebChart, DateTimeFormatOptions, NumberFormatOptions } from "@arcgis/charts-spec";
/**
 * Formats a number or date
 * @param props.value
 * @param props.valueFormat
 * @param props.iLocale
 * @param props.formatLocale
 */
export declare function format(props: {
    value: Date | number;
    valueFormat: DateTimeFormatOptions | NumberFormatOptions;
    iLocale: ILocale;
    formatLocale?: string;
}): string;
/**
 * Indicates whether or not the chart is rotated.
 * @param props.chart the chart
 * @param props.config the chart config
 */
export declare function isChartRotated(props: {
    chart?: XYChart;
    config?: WebMapWebChart;
}): boolean;
/**
 * Disposes a specific storage. To be used when a chart is disposed for instance.
 * @param chart The chart containing the references to all the element storing information
 */
export declare function deleteChartStorage(chart: Chart): void;
/**
 * Converts an amCharts padding to a value
 * @param padding The padding formatted as number or Percent
 * @returns The padding value
 */
export declare function getPaddingValue(padding: Percent | number): number;
